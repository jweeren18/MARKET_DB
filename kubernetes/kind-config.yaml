# Kind cluster config: 1 control-plane + 2 worker nodes
# Usage: kind create cluster --name market-intel --config kubernetes/kind-config.yaml
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
  - role: worker
  - role: worker
